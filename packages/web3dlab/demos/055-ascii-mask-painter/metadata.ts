import type {DemoMetadata} from '../../types';

export const asciiMaskPainterMetadata: DemoMetadata = {
  number: '055',
  id: 'ascii-mask-painter',
  title: 'ASCII Mask Painter',
  description: 'An ascii-magic v2 inspired masking tool mock where pointer paint controls glyph density, image silhouette, and editable character regions.',
  route: '/demos/ascii-mask-painter',
  tags: ['Canvas2D', 'ASCII', 'Masking', 'Design Tool'],
  instructions: ['Move or drag over the canvas to paint the mask, then tune density and brush size to review ASCII as editable material.'],
};
